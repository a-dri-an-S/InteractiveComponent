import CardInteractive from './CardInteract';
import "../styles/Card.css";

const Card = () => {
    return (
        <section className="main-card">
            <CardInteractive />
        </section>
    );
}

export default Card;